<UserControl x:Class="storage_tracker.Views.DictionaryGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <Style x:Key="GridButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="DangerGridButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#D32F2F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#B71C1C"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Toolbar -->
        <Border Grid.Row="0" Margin="0,0,0,10" Padding="0,0,0,10" BorderBrush="#BDC3C7" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Content="➕ Добавить" Command="{Binding AddCommand}"
                        Style="{StaticResource GridButtonStyle}"/>
                <Button Content="✏️ Редактировать" Command="{Binding EditCommand}"
                        Style="{StaticResource GridButtonStyle}"/>
                <Button Content="🗑️ Удалить" Command="{Binding DeleteCommand}"
                        Style="{StaticResource DangerGridButtonStyle}"/>
            </StackPanel>
        </Border>
        
        <!-- DataGrid -->
        <DataGrid x:Name="MainDataGrid" Grid.Row="1" ItemsSource="{Binding Items}"
                  SelectedItem="{Binding SelectedItem}"
                  IsReadOnly="True" AutoGenerateColumns="True"
                  CanUserSortColumns="True" 
                  BorderBrush="#BDC3C7" BorderThickness="1"
                  RowHeaderWidth="0"
				  AutoGeneratingColumn="DataGrid_AutoGeneratingColumn">
            <DataGrid.Resources>              
            </DataGrid.Resources>
        </DataGrid>
        
        <!-- Status -->
        <Border Grid.Row="2" Margin="0,10,0,0" Padding="10,8" Background="#F8F9FA" CornerRadius="4">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="📊 Статус: " FontWeight="SemiBold" FontSize="12"/>
                <TextBlock Text="{Binding StatusMessage}" Foreground="#007ACC" FontSize="12"/>
                <TextBlock Text=" | " Margin="10,0" FontSize="12"/>
                <TextBlock Text="📋 Записей: " FontWeight="SemiBold" FontSize="12"/>
                <TextBlock Text="{Binding Items.Count}" Foreground="#2C3E50" FontSize="12" FontWeight="Bold"/>
            </StackPanel>
        </Border>
        
        <!-- Loading Overlay -->
        <Border Grid.Row="0" Grid.RowSpan="3" Background="#FFFFFF" Opacity="0.8"
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                CornerRadius="8">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="⏳ Загрузка данных..." FontSize="18" Foreground="#007ACC" FontWeight="SemiBold"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>